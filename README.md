# PaiNai(ไปไหน?)

**PaiNai** เป็นโปรเจคเว็บแอปพลิเคชันที่ใช้ปัญญาประดิษฐ์ (AI) เพื่อให้คำแนะนำเกี่ยวกับการเดินทาง โดยเฉพาะอย่างยิ่งสำหรับผู้ที่ต้องการสอบถามเส้นทางหรือข้อมูลท่องเที่ยวที่น่าสนใจ

Live demo: [pai-nai.vercel.app](https://pai-nai.vercel.app)

## คุณสมบัติหลัก

- 🧭 ระบบ AIVoiceChat แบบเรียลไทม์
- 🗺️ ให้คำแนะนำเส้นทางและวิธีการเดินทางที่สะดวกและปลอดภัย
- 🔄 บันทึกข้อมูลการสอบถามให้ผู้ใช้ดูย้อนหลังได้

## โครงสร้างโปรเจค

โฟลเดอร์หลักประกอบด้วย:

- `app/` - ไฟล์หน้าและโครงสร้าง Next.js
- `components/` - คอมโพเนนท์ UI ที่ใช้ในระบบ
- `api/` - โค้ด backend สำหรับจัดการเราต์และข้อมูล
- `config/` - การตั้งค่าเช่น database, model AI
- `context/`, `lib/` ฯลฯ สำหรับสภาพแวดล้อมของ React

## เริ่มต้นใช้งาน

1. ติดตั้ง dependencies:
   ```bash
   npm install
   ```
2. ตั้งค่า environment variables และฐานข้อมูลตาม `config` ที่ต้องการ
3. รันแอปพลิเคชัน:
   ```bash
   npm run dev
   ```

## เทคโนโลยีที่ใช้

- Next.js (front-End + Back-End)
- Tailwind CSS (หรืออื่น ๆ ตามที่ใช้)
- Vapi Ai (สำหรับ Ai voice chat)
- OpenrouterAi (Ai สำหรับสรุปบทสนทนา)
- Neon Postgres (เก็บข้อมูลผู้ใช้ มี 2 ตาราง user กับ reports)
- Clerk authentication (ระบบ login)

---

